# Transport map - forward all mail to the backend service
# This is used with hash: lookup, for simple domain-based forwarding
# Format: domain    transport:destination
# The backend host is configured via the POSTFIX_BACKEND_HOST environment variable
nnu.edu.kg      smtp:[BACKEND_HOST]:2525