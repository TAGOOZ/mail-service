# ⚠️  DEPRECATED: Backend-specific .env file
# 
# This file is kept for reference only.
# All environment variables are now managed in the root directory's .env file.
# 
# To set up the development environment:
# 1. Copy ../env.example to ../.env
# 2. Modify the values in ../.env as needed
# 
# The backend will automatically read environment variables from the root .env file.

# For reference, backend uses these environment variables:
# PORT, NODE_ENV, FRONTEND_URL, JWT_SECRET, JWT_EXPIRES_IN
# MONGODB_URI, REDIS_URL, SMTP_HOST, SMTP_PORT, MAIL_DOMAIN, MAIL_PORT
# LOG_LEVEL, RATE_LIMIT_WINDOW_MS, RATE_LIMIT_MAX_REQUESTS
# BCRYPT_ROUNDS, SESSION_SECRET, CORS_ORIGIN